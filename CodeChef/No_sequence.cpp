#include <bits/stdc++.h>
#define int long long int
using namespace std;
void solve(){
    int n, k, s;
    cin >> n >> k >> s;
    if(n%k!=0 && (n-1)%k!=0){
        cout<<-2<<endl;
        return;
    }
    for (int i = 0; i <= n;i++){
        for (int j = i; j <= n;j++){
            for(int k)
        }
    }
}

signed main(){
ios_base::sync_with_stdio(0);
cin.tie(0);
int test;cin>>test;
while(test--) solve();
 }