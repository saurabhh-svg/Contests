#include <bits/stdc++.h>
#define int long long int
using namespace std;
void solve()
{
    int w, d, h, ans = 0, a, b, f, g;
    cin >> w >> d >> h >> a >> b >> f >> g;
    int x = min(w - a, a);
    int y=min(h-b,b);
}

signed main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int test;
    cin >> test;
    while (test--)
        solve();
}