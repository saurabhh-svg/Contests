#include <bits/stdc++.h>
#define int long long int
using namespace std;

void solve(){
    int n;
    cin >> n;
    vector<vector<int>> data(n,vector<int>(2,0));
    for (int i = 0; i < n;i++){
        cin >> data[i][0];
        cin >> data[i][1];
    }

    vector<int> pref(n);

    for (int i = 0; i < n;i++){
        
    }

        int q;
    cin >> q;
    while(q--){
        int x;
        cin >> x;

    }
}

signed main(){
ios_base::sync_with_stdio(0);
cin.tie(0);
int test;cin>>test;
while(test--) solve();
 }